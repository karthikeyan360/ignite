import { ValidactionDirective } from './validaction.directive';

describe('ValidactionDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidactionDirective();
    expect(directive).toBeTruthy();
  });
});
